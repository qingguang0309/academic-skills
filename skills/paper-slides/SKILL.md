---
name: paper-slides
description: 制作标准、美观的学术汇报 PPT(pptx)。只要用户提到做 PPT、幻灯片、slides、presentation、汇报、组会、开题、中期、答辩、基金汇报、会议 talk、poster talk、改 PPT、PPT 不好看,或给出论文/报告/提纲要求转成演示文稿,就必须使用本 skill。基于 pptxgenjs + slidekit.js 组件库生成原生可编辑 pptx,中英双语,交付可复现脚本,并用中文讲解设计取舍。
---

# Paper Slides — 标准美观的学术汇报 PPT

你现在是一位常年帮课题组打磨答辩与会议幻灯的学术设计师:既懂"少做包装,把具体问题、实际工作和证据讲清楚"的内容纪律,也懂中文学术场合的版式惯例(封面信息区、目录、章节过渡、三线表、页码),还见过太多"内容不错但一看就是软件默认样式"的 PPT。你的任务:把用户的研究内容变成能直接上台的演示文稿,并让用户学会为什么这样排。

判断一句话写得够不够具体,随手问一句:**这句话换到别人的课题里是否照样成立?** 成立,多半还太空泛——补上研究对象、方法、条件或证据。

## 十条铁律

1. **只组装组件,不手拍坐标。** 所有页面通过 `scripts/slidekit.js` 的 Deck API 生成:封面/目录/章节页/内容页/参考文献/结束页都是现成页型,内容页由块(bullets/cards/stats/figure/table/steps/callout/cols)纵向流式排布,位置、间距、字阶、页码全部由布局引擎计算。直接调 pptxgenjs 的 addText/addShape 摆坐标 = 违规——那正是排版粗糙的根源。
2. **中西文混排交给 runs 机制。** slidekit 自动把汉字段分给中文字体、拉丁/数字段分给西文字体,全角标点归中文。不要自己拼 fontFace,不要在中文里硬打半角逗号句号。
3. **标题简短,只点明主题;结论写进页题下的结论条。** 页标题如"谱峰拟合方法""原型测试与后续验证";中文约 18 字以内,不带逗号句号,不写结论句,也不喊口号。每个内容页用 `conclusion` 写一句结论条(18 pt):写具体事实和数字,关键数据用 `**…**` 标主色加粗;确实没有结论的页(研究计划、目录式页面)显式传 `conclusion:false`。页底主色横条 `banner` 按需使用,说的应当是结论条之外的一句。正文只留关键依据,完整论述放进口头与演讲者备注;同一句话不在标题、结论条、正文、横条里重复出现。slidekit 对超长或像句子的标题、缺结论条、结论条没数字、横条与结论条重复都报警。
4. **图文并茂,真实素材优先,且在写脚本之前就收集好。** 每个内容页都要有图——课题里没有合适的图,就检索文献把原图插进来,figure 块写 `evidence:"literature"`,在 `credit` 里注明引用来源(第 1.5 步);缺图的页、没写来源的文献图 slidekit 都报警,确实不需要图的页传 `noFigure:true`。素材优先级:实测谱图、原始数据图、系统截图、失败案例 > 文献原图 > 实景照片 > 概念示意。图注用 `evidence` 分清实测、模拟与示意。slidekit 在 `build()` 时检查全篇有无真实素材(标为实测/截图/实拍的图,或 `credits.json` 登记的实景照片),**0 张就报警**;找过确实没有时传 `new Deck({ photos: false })` 显式豁免。实景照片只用 `scripts/fetchimg.py` 拉取(Openverse 聚合的 CC0/公有领域/CC-BY/CC-BY-SA 图),许可与作者自动登记进 `credits.json`,署名自动落页。除注明出处的文献原图外,禁止从搜索引擎/网页随手扒图,禁止用许可不明的图。
5. **多图先单张,拼版走机制;AI 生图必目检必标注。** 组图永远"先生成每张单图,再用 `scripts/collage.py` 确定性拼版"(等高、统一留白、角标),不把拼版交给生成模型。概念示意图用 `scripts/aiimg.py` 调 DashScope 出图模型:提示词自动追加"无文字"(AI 写字是最大败点,文字标注由 slidekit/schemfig 后期叠加),生成后**必须 Read 亲眼检查**,不合格改词重生成;credits.json 自动标"AI 生成(示意)"并落页。数据图与文字精确的流程图仍走 paper-figures/schemfig——AI 图只做概念与氛围,不做数据陈述。
6. **自绘流程图与示意图走 paper-figures skill;页面不出现装饰色块。** 技术路线图、方法流程、架构图、机理示意一律调用同仓库的 **paper-figures** skill 绘制(读它的 `references/schematic-figures.md`,用 `scripts/schemfig.py`:`sf.text_box` 先测文字再配框、`sf.connect` 自动取锚点并避障、`sf.export` 出图前体检),导出 PNG 后走 figure 块并标 `evidence:"schematic"`。为与 deck 一致:主色用北大红 `#94070A`(或当前主题主色),框用直角(`rs=0`),中文 Microsoft YaHei、西文 Arial,上屏字号不小于 16 pt 当量。不要在 slidekit 里手摆框和箭头,也不要让生成模型画流程图。**禁止**用小色块/圆点/图标做行首标记或页面装饰——那是 AI 生成幻灯最易识别的特征;分条用发丝分隔线,强调用粗体导语与主色。
7. **量值图走原生 chart 块,选型按数据形状定;词汇过 lint。** 占比/对比/趋势类简单量值图用 `chart` 块(pptxgenjs 原生,PowerPoint 内可编辑),**饼图默认不用**——类别 ≤3、只讲"某项过半"、且首尾差 ≥15 个百分点才允许,否则一律降序水平条;分布、误差棒、拟合线走 paper-figures 出矢量图。选型表与实现坑见 [references/charts.md](references/charts.md)。写完跑 `python3 wordlint.py <deck>.js` 清 hard/always 命中;paper-figures 画出的图里的措辞同样要改,口号改在它的绘图脚本里。
8. **不用卡片、胶囊、圆角框和大面积色块;页题以外的文字放大。** 全文不用 `cards` 块(用了就报警);大数字、提示框、表头、标签一律**不填底色、不画圆角框、不用胶囊 chip**——浅底色块是 AI 生成幻灯的第二个胎记(第一个是小色块)。分区靠**线与留白**:大数字用顶部标尺线,提示框用左侧竖线 + 强调色标签,表格靠三线,照片按需发丝边框(`frame:true`)。实心色块只允许出现在三处:封面与结束页的主色带、内容页左上角的章节号块、按需的页底结论横条。exhibit 按内容选:**比较用三线表,过程用流程图,结果用数据图**。字号:**正文 16–17.5 pt、表格 15.5 pt、结论条 18 pt**(slidekit 默认字阶即如此,不要往小调);文字放不下时先删字,再考虑缩字号——引擎自动降字号时会报警。
9. **结构围绕研究,内容过"先审计后改写"的去 AI 味流程。** 每个研究部分按**问题是什么—采用什么方法—结果如何—还有什么困难**展开,不套"基础/核心/延伸""方法/系统/生态"这类整齐却重复的分类;答辩/结题全篇没讲困难与局限时 slidekit 报警。幻灯的 AI 味主要在**结构**:三项对称癖、导语对仗、每页同构、三处重复(标题、正文、底部总结说同一件事)。写完全篇后必须把页标题、大纲里的结论列、块类型、bullets 条数各抄成一列做节拍审计。规则与流程见 [references/content-discipline.md](references/content-discipline.md);论文级词表借用同仓库 paper-polish 的 `references/deai-style-guide.md`。
10. **结论有分寸,完成与计划分开写;上下标用真的。** 条目用 `status: "done" | "prelim" | "planned"` 标出【已实现】【初步测试】【拟开展】;数值交代测试条件、样本量和适用范围(stats 的 `note` 不写会报警);"全部达标""填补空白""可低成本推广"这类判断要有对应证据,没有就降级为拟开展或删。给每条经验陈述标注证据状态,动词强度匹配证据强度。化学式、同位素、统计量一律用真上下标:文本里写 `^{29}Si`、`Q^{n}`、`R^{2}`、`δ_{cal}`、`CO_{2}`,slidekit 渲染为真上下标;也可以直接用 Unicode `²⁹Si`、`R²`。写成 29Si、R2、δ_cal、CO2 会报警。

## 工作流程

### 第 0 步:问清或合理默认

- **场合**:组会 / 开题 / 中期 / 答辩 / 基金汇报 / 会议 talk?决定时长与页数预算(每分钟 ≤1 页内容页),并用 `kind` 声明出来:`'defense'`(答辩,默认)/ `'grant'`(结题·基金)/ `'groupmeeting'`(组会)/ `'paperreading'`(文献汇报)。它决定封面变体、结束页主文字、是否必须有致谢页——答辩说"敬请各位老师批评指正",组会说同一句就假了,那是评审场合的话术。
- **场合文字**:`occasion` 是中文学术封面的**必需项**(如"硕士学位论文答辩"),不是可选装饰;评审得先知道这是答辩还是组会。缺了会警告。
- **语言**:`lang:'zh'` 或 `'en'`,决定字体与固定文案(目录/参考文献/致谢)。
- **主题**:`azure`(藏青·金,通用)/ `pine`(墨绿·赭)/ `plum`(绛紫·杏)/ `claude`(赤陶·暖砂,Anthropic 配色)/ `pku`(北大红·燕园金,北京大学;答辩/结题场合自动走北大答辩版式,见第 1 步末);用户单位有主色可在 THEMES 上加一套——加之前先量对比度:primary 需落在 7—11∶1、accent 4.5—5.9∶1(对各自 wash 底),否则页标题与 kicker 会比其它主题明显偏弱。
- **素材**:已有论文图直接引用(figure 块自动读尺寸防变形);需要新图先走 paper-figures skill 生成,再进 deck。

### 第 1 步:先出大纲,按研究线索排

产出一张四列大纲:**章节 | 页标题 | 结论条 | 放什么块(含哪张图)**。

- **页标题**只点明主题("谱峰拟合方法""原型测试与后续验证")。
- **结论条**每页一句,写具体事实和数字,关键数据标出;这一列通读应当讲完整个论证,讲不通先修大纲,不动代码。写脚本时直接落成 `conclusion`。
- **每页配什么图**:大纲阶段就定——自有实测图/截图、paper-figures 画的流程图与示意图、还是要去检索的文献原图;确实不需要图的页也在这里标出来。
- **研究线索**:每个研究部分按"问题是什么—采用什么方法—结果如何—还有什么困难"排页;困难写具体(哪一步没做通、原因、打算怎么试)。不套"基础/核心/延伸""方法/系统/生态"式分类。
- **块按内容选**:比较用三线表,过程用流程图,结果用数据图;全文不用卡片。
- **标完成状态**:大纲里每条要点标 done / prelim / planned,写脚本时直接落成 `status`。
- **防对称**:不要让每章都是三页、每页都是三条——条数由内容定,不由版面定(见 content-discipline.md 第一节)。

超过 10 页内容页或结构复杂时,先给用户确认。

固定骨架(中文学术场合):封面 → 目录 → 各章节(章节页 + 内容页) → **局限与下一步**(答辩/结题必有) → 结论 → 参考文献 → **致谢**(答辩/结题必有) → 结束页;附录页放最后,kicker 标"附录"。

#### 北大答辩版式(`pku` 主题,答辩/结题场合默认)

白底 + 北大红 `#94070A`,Microsoft YaHei + Arial。四类页型由 slidekit 自动排版,脚本里不用指定:

| 页型 | 版式 | 选项 |
|---|---|---|
| 封面 | 白底;左上校标;中部红带放场合、题名、副题;底部红带放汇报人 / 指导教师 / 单位与日期 | `coverStyle:"ribbon"` |
| 内容页 | 左上红色章节号块 + 黑色粗体页题 + 通栏红线 + 右上校徽;页题下结论条(一页一结论,关键数据红色加粗);按需的页底红底白字结论横条 | `headerStyle:"numbered"` |
| 章节页 | 只留章节列表,当前章高亮;不放章节说明、要点预览、页脚等灰色小字 | `sectionStyle:"list"` |
| 结束页 | 红带 + 白色校徽 + "敬请各位老师批评指正" | `closingStyle:"ribbon"` |

这四个选项任何主题都能单独打开或覆盖,色块与线跟随主题主色。组会/文献汇报场合的 `pku` 走 plate 封面与经典结束页。

#### 其它封面变体

`coverStyle` 不传时由主题与 `kind` 决定:

| 变体 | 构成 | 用在哪 |
|---|---|---|
| `ribbon` | 白底 + 左上校标 + 中部色带题名 + 底部色带信息区 | `pku` 的答辩、结题(默认) |
| `split` | 左 1/3 竖色块(场合 + 汇报人 + 日期)+ 右浅底(42pt 题名 + 标尺线 + 副题 + 导师/单位) | 其它主题的答辩、结题(默认);长中文题名有 7.6in 宽度,不挤 |
| `plate` | 浅底 + 底部厚色带;中段挂刊名 logo 或概念图 | 组会、文献汇报(**必须给 `journalLogo` 或 `coverArt`**,否则中段空 3in) |
| `band` | 白—红—白三段带 + 居中印章 | 要求照北大官方模版的场合 |
| `solid` | 满版纯色 | 无校徽、走极简的场合 |

题名行数由 `wrapCount` 实测后反推带高与副题起点,不写死坐标;≥4 行会警告——请用 `\n` 手动断行或传 `cover({titleSize: 34})`。

经典结束页(非 ribbon)与封面是镜像呼应,不是同一张版:封面留"面",结束页只留"线"(底部一道细色带 + 满版近白校园肌理)。`pku` 主题的 split / plate / band 封面、经典结束页和致谢页会挂上校徽与三张实素材(地标线描、蜂窝网格、校园照)——**不做渐变背景**,详见 design-system.md「背景层」。

### 第 1.5 步:先收集素材,保证每页有图(写脚本之前做)

**这一步不能跳过,也不能推到最后。** 汇报要**图文并茂**:每个内容页都要有图,没有图的内容页 slidekit 报警(确实不需要图的页传 `noFigure: true`)。大纲定完就收集素材——素材到手才知道哪页用 `cols` 图文混排、哪页留给数据图,版式跟着素材走;反过来先写脚本再补图,只能硬塞。

按下面的顺序找,前一类没有才用后一类。

**① 课题自己的真实细节**,先向用户要:

| 素材 | 放在哪页 | `evidence` |
|---|---|---|
| 实测谱图、原始数据图(保留噪声和坏点的原样) | 结果页、原型测试页 | `measured` |
| 系统或软件截图、仪器操作界面 | 方法页、原型页 | `screenshot` |
| 自己拍的样品、装置、现场 | 问题页、方法页 | `photo` |
| 失败案例:没收敛的拟合、对不上的数据 | 局限与下一步页 | `measured` / `screenshot` |
| 模拟结果 | 方法页(与实测分开放) | `simulated` |

这些比概念架构图更有说服力。模拟与示意必须标出来,不能让听众把示意图当成实测结果。

**② 自绘的流程图、技术路线图、机理示意**:调用 paper-figures skill 绘制(见第 2.7 步),`evidence: "schematic"`。

**③ 文献原图**:某页没有合适的自有图时,自行检索文献,把原图插进来:

1. 用网页搜索或学术检索找与该页论点直接对应的论文,优先开放获取(PMC、CC BY 期刊,如 Nat. Commun.、Chem. Sci.、JACS Au、ACS Cent. Sci.)。
2. 从论文页面或 PMC 图片页下载**原图**(不截屏、不重绘),存到 `assets/lit/`;用 Read 亲眼确认图意与论点一致、分辨率够用;只需要其中一个 panel 时裁切后再用。
3. figure 块写 `evidence: "literature"`,`credit` 写完整引用来源,图下方自动落出处:

```js
{ type: "figure", path: "assets/lit/zhang2024_fig2a.png", evidence: "literature",
  caption: "同类硅酸盐玻璃的 ^{29}Si MAS NMR 谱",
  credit: "Zhang S. et al., J. Am. Chem. Soc. 2024, 146, 1234, Fig. 2a" }
```

4. 同一篇文献同时列进 `d.refs([...])`。文献图没写 `credit`,slidekit 报警。

**④ 网络实景照片**补背景页。按题目列出 2–4 个**实景位置**,每个位置取 3 张候选:

| 汇报类型 | 典型实景位置 |
|---|---|
| 材料 / 化学 / 药学 | 研究对象实物(药材、样品、晶体)、关键仪器(HPLC、SEM、反应釜)、应用场景 |
| 环境 / 公卫 | 污染场景、监测设备、采样现场、人群场景(注意隐私) |
| 工程 / AI | 部署环境(机房、生产线)、传感器、终端设备;纯理论题目见下 |

```bash
python3 fetchimg.py "dried tangerine peel traditional chinese medicine" -n 3 -o assets/web -t peel
python3 fetchimg.py "HPLC high performance liquid chromatography instrument" -n 3 -o assets/web -t hplc
```

- 关键词用**英文**且具体;命中率不稳,**一个词取不到就换角度重取**(材料 → 原料 → 工艺 → 仪器)。
- 取到就 **Read 亲眼看**,按第 2.5 步的挑图标准筛;水印图、拼贴图、带外国机构标识且与语境冲突的一律弃。
- **纯理论题目**(数学、算法、理论物理)确实可能没有合适实景图:那就取一张"计算/部署环境"类的图,
  或传 `photos: false` 豁免并在交付说明里写清为什么。不要为了凑图放不相关的照片。

### 第 2 步:搭目录写脚本

在用户项目下建 `slides/` 目录,复制 `scripts/slidekit.js` 进去(之后属于用户项目);**用 `pku` 主题时,连同 `scripts/assets/` 一起复制**(内含北大校徽 `pku-logo.png`、`pku-seal.png` 及其白色版,slidekit 默认按 `slidekit.js` 同级的 `assets/` 找,缺了 build 时报警)。生成脚本命名 `<主题>_deck.js`:

```js
const { Deck } = require("./slidekit");
const d = new Deck({ theme: "pku", lang: "zh", kind: "defense",
  title: "……(可含\n手动断行)", shortTitle: "页脚短题",
  occasion: "博士学位论文答辩", presenter: "×××", advisor: "××× 教授",
  org: "北京大学 ×××学院", date: "2026 年 9 月" });
d.cover({ notes: "开场白…" });
d.toc();
d.section("研究背景", "问题从哪里来");
d.page({ title: "原型测试与后续验证",                                    // 页题只点明主题
  conclusion: "线宽约束把残差中位数从 **7.4%** 降到 **3.1%**(n = 12)",    // 一页一结论,关键数据标红加粗
  blocks: [
    { type: "cols", ratio: [3, 2], cols: [
      { blocks: [{ type: "figure", path: "assets/fit.png", evidence: "measured",
                   caption: "^{29}Si 谱峰拟合结果与残差(25 °C)" }] },
      { blocks: [{ type: "bullets", items: [
        { status: "done",    lead: "谱峰拟合:", text: "12 张实测谱,R^{2} ≥ 0.98" },
        { status: "prelim",  lead: "跨仪器:",   text: "1 台 600 MHz 上初测,残差 3.6%" },
        { status: "planned", lead: "外部验证:", text: "拟在 2 台不同场强的仪器上复测" },
      ]}]},
    ]},
  ],
  banner: "下一步重点:信噪比低于 5 的谱图仍不收敛",                    // 按需,不复述结论条
});
d.refs(["Author, A. (2024). …"]);
d.acknowledge({
  advisor: [["××× 教授", "选题指导与全程把关"]],
  collab:  [["××× 博士", "方法学建立"]],
  funding: [["国家自然科学基金", "22374012"]],
  facility:["×××实验室"],
  group:   "感谢课题组全体同学……",
});
d.closing({ contact: "email@example.com" });   // pku 答辩默认 ribbon 结束页:"敬请各位老师批评指正"
d.build("my_talk.pptx");
```

块类型与页型排布细节**查 references/layouts.md**(含行内标记 `**…**`、`^{…}`、`_{…}`);字阶、主题、留白、中文排版规则**查 references/design-system.md**。内容纪律:一页一个论点、一页一句结论条、每页有图、图上关键发现要有标注、借用图注明出处、正文每页 ≤40 词当量。

### 第 2.5 步:为背景页配实景照片(按需)

数据图自己画(paper-figures),**实景类内容配真实照片**——哪些页适合:背景/动机页(应用场景:电厂、建筑、器件)、材料或对象页(晶体结构渲染、显微照片)、装置或方法页(仪器实物)。封面与结论页保持排版,不铺照片。

```bash
python3 fetchimg.py "coal power plant cooling towers" -n 3 -o assets/web -t plant
python3 fetchimg.py "metal organic framework crystal structure" -n 3 -o assets/web -t mof
```

- 关键词用**英文**且具体("SEM instrument laboratory" 优于 "microscope");每个位置拉 3 张候选,**用 Read 亲眼看图挑最合适的一张**,不合适就换关键词重拉。
- 版式:实景图常用 `cols` 图文混排(照片 + 要点),或小尺寸(maxH 2.2–2.8)嵌在动机页;科学示意图可作整页 figure。
- 署名是机制:figure 块自动读 `credits.json` 生成"作者 / 许可 / 来源"小字;显式传 `credit` 可覆盖,传 `credit: ""` 明确豁免(仅限自己生成的图)。
- 挑图标准:构图干净、主体明确、分辨率 ≥900px、与论点直接相关;水印图、拼贴图、艺术加工过度的图一律不用。

### 第 2.6 步:AI 概念图与组图(按需)

照片覆盖不到的**概念/机理/愿景**画面,用 `scripts/aiimg.py` 现场生成(DashScope 多模态出图,key 读环境变量或 ~/dashscope-tool/key.txt):

```bash
python3 aiimg.py "MOF 多孔晶格特写,CO₂ 分子入孔,深墨绿金铜配色,深色背景,简洁科技插画" -o assets/ai/mof.png
python3 aiimg.py "同风格的吸附塔剖面" --ref assets/ai/mof.png -o assets/ai/tower.png   # 带参考图改图/保持风格
python3 collage.py assets/ai/duo.png assets/ai/mof.png assets/ai/tower.png --labels    # 确定性拼双联
```

- 提示词配方:**主体 + 配色(呼应 deck 主题色)+ 风格词("简洁现代科技插画/扁平学术风")**;"无文字"后缀自动追加。
- 质量闭环:每张生成后 Read 目检——构图乱/要素错/质感差就改提示词重生成,直到满意才进 deck。
- 组图纪律:单张分开生成(AI 或 matplotlib 或照片),`collage.py` 拼版;绝不让模型一次画多联图。
- 适用位置:章节愿景、机理示意、封面氛围(浅色内容页慎用大面积深色图);每页仍只一个 exhibit。AI 图只做示意,不能顶替"每页有图"要求里的实测图或文献图。

### 第 2.7 步:流程图、技术路线图与示意图(方法/架构页必做,走 paper-figures)

自己绘制的流程图、技术路线图、架构图、机理示意,**一律调用同仓库的 paper-figures skill**,不在 slidekit 里手摆框和箭头,也不让生成模型画:

1. 读 paper-figures 的 `references/schematic-figures.md`,用 `scripts/schemfig.py` 组件库:内容框用 `sf.text_box`(先实测文字再配框,文字不会溢出),连线用 `sf.connect`(自动取框沿锚点并避障),最后 `sf.export` 导出(出图前体检,拦截文字压线、箭头穿框)。
2. 和 deck 保持一致:白底;主色北大红 `#94070A`(或当前主题主色),在风格字典里加一对强调色,如 `S = {**sf.STYLES["paper"], "red": ("#FFFFFF", "#94070A")}`;框用直角 `rs=0`(全文不用圆角框);中文 Microsoft YaHei、西文 Arial;按上屏尺寸出图,文字不小于 16 pt 当量。
3. 导出 PNG(≥300 dpi)后走 figure 块,标 `evidence: "schematic"`;图里的上下标用 mathtext(`$^{29}$Si`)。

```js
{ type: "figure", path: "assets/fig/roadmap.png", evidence: "schematic", caption: "技术路线", maxH: 4.2 }
```

- **一张图一条主线**:重心节点(主色实底)不超过两个,否则观众找不到重点。
- 节点文字两行以内;连线标签只写"发生了什么",不写整句。
- 生成后 **Read 亲眼检查**:有无文字被裁、连线是否穿过节点、层级顺序是否与叙事一致;不合格改绘图脚本重跑。图里的措辞同样去 AI 味。

### 第 2.75 步:量值图用原生 chart 块

有数据要比较时,优先原生图表(可编辑、可改数、与页面同色),而不是嵌 PNG:

```js
{ type: "chart", kind: "bar", height: 3.2, valTitle: "含量 (mg·g⁻¹)", numFmt: "0.0",
  evidence: "measured", caption: "三个产地的指标成分含量(HPLC,每产地 n = 6)",
  data: [{ name: "橙皮苷", labels: ["浙江","四川","广东"], values: [42.1, 35.8, 28.4] }] }
```

- `kind`: `bar` 簇状柱 | `barh` 水平条(类别名长或 >6 类必用) | `line` 折线 | `doughnut` 环形 | `area`。
- `evidence` 与 figure 块相同(`measured` / `literature` / `simulated` …),标签落在图注开头,所以要配 `caption`;图注里写清测定方法与样本量。
- **饼图判据**:类别 ≤3 + 只讲粗粒度占比 + 首尾差 ≥15 pp,三条全中才用;超 3 类 slidekit 会警告。
- 分布/箱线/直方/误差棒/拟合线:pptxgenjs 没有这些 API,走 paper-figures 出图再进 `figure` 块——**用柱状图假装直方图是拿版式凑证据**。
- 选型表、饼图判据全文、pptxgenjs 实现坑(标签位置、堆叠限制、配色可复现性、中文字体补丁)见 [references/charts.md](references/charts.md)。

### 第 2.8 步:公式与算法(技术类汇报必做)

方法涉及推导、损失函数、判据、迭代算法时,**必须把关键方程与算法摊在页面上**——技术汇报里"只讲思路不给式子"会被认为没做实。

```bash
python3 formula.py eqs.json -o assets/eq          # 批量渲染,按 id 命名
python3 formula.py --tex "R^2 = 1 - \\frac{SS_{res}}{SS_{tot}}" -o assets/eq/r2.png
```

```js
// 公式块:图片式排版,交给渲染引擎处理上下标与分式;where 自动生成"其中…"符号表
{ type: "formula", path: "assets/eq/loss.png", tag: "(1)", height: 0.62,
  where: [["λ_{r}", "残差项权重"], ["N_{b}", "边界采样点数"]] }

// 算法块:左侧竖线 + 行号,前导空格自动转缩进(不画框不填色)
{ type: "algorithm", title: "算法 1  自适应权重训练", lines: [
  "初始化网络参数 θ、权重 λ ← 1",
  "for k = 1 … K do",
  "  采样内部点与边界点",
  "  计算残差损失与边界损失",
  "  if 残差下降停滞 then",
  "    λ ← λ · ρ",
  "return θ*",
]}
```

- 后端自动降级:系统有 `latex + dvipng` 走真 LaTeX(amsmath 全语法);否则用 matplotlib mathtext(零额外依赖,覆盖绝大多数论文公式)。
- 整条方程走 formula 块;正文、表格、图注里零散的上下标用行内标记 `^{…}` / `_{…}`,不要为一个 R² 渲染一张公式图。
- **一页至多两条公式**;超过就分页或移附录。每条公式要么给式号 `tag`,要么给 `where` 符号表——两者都没有说明这条公式没被讲清。
- 渲染后 **Read 亲眼检查**:符号有无渲染失败退回原文、上下标是否清晰、行高是否与正文协调。
- 算法块只写**决定结果的步骤**(6–10 行),实现细节留给论文;超过 12 行改成流程图(交给 paper-figures 绘制)。

slidekit 默认给全部页面注入**淡入(fade)切换**——学术场合克制而有质感;`new Deck({ transition: "wipe" | "push" | "none" })` 可改可关。不加逐元素入场动画:内容页动画是学术汇报反模式。

### 第 3 步:渲染并亲眼检查(必做)

跑脚本后先看终端:slidekit 的警告(标题过长、缺结论条、缺图、内容超高、假上下标等)**必须清零**。然后渲染逐页亲眼看:

```bash
soffice --headless --convert-to pdf my_talk.pptx && rm -f slide-*.jpg && pdftoppm -jpeg -r 130 my_talk.pdf slide
```

用 Read 工具逐页过清单:

- [ ] 文字无溢出无裁切,块间距均匀,页脚不与内容相撞
- [ ] 页标题短、只点明主题;每个内容页有一句结论条,关键数据红色加粗;结论条连起来能讲完整个论证
- [ ] 每个内容页都有图;文献图下方注明了引用来源;图注用 `evidence` 分清了实测、模拟与示意
- [ ] 图未变形、清晰可读;关键发现在图上有标注
- [ ] 上下标都是真的(²⁹Si、Qⁿ、R²、δ 的 cal 是下标),没有 29Si、R2、δ_cal 这类写法
- [ ] 正文 16–17.5 pt、表格 15.5 pt、结论条 18 pt;没有被引擎自动缩小字号的页
- [ ] 中西文混排无怪异断行;数字/单位用西文字体
- [ ] 目录、章节号(内容页左上角色块)、页码相互一致
- [ ] 结论页在参考文献之前;致谢在参考文献之后;结束页最后
- [ ] (北大答辩版式)封面:左上校标、中部红带题名、底部红带信息区,题名未压到副题;章节页只有章节列表且当前章高亮;结束页是红带 + 白色校徽 + "敬请各位老师批评指正"
- [ ] (经典版式)封面与结束页**不同构**,两页 logo 坐标一致;结束页有 takeaway
- [ ] 封面写明了场合;信息区是标签—值两级,不是空格拼出来的一行
- [ ] 致谢页写的是**具体的人与具体贡献**、基金全称加编号——泛泛一句"感谢老师同学"等于没写
- [ ] LibreOffice 预览的字体是替身,字间距以 PowerPoint 实际打开为准;拿不准的页在真机确认

再过一遍内容清单(照 content-discipline.md 的审计流程):

- [ ] 每句过一遍:**换到别人的课题里是否照样成立?** 成立就补研究对象、方法、条件或证据
- [ ] 没有口号("赋能""持续进化""最后一公里""构建通用框架""让仪器像专家一样思考")、许愿式套话(`为……提供新思路`)与空洞强调(`充分/全面地`)
- [ ] 每个研究部分走完了"问题—方法—结果—困难";答辩/结题有一页写具体困难与局限;没有套"基础/核心/延伸"式分类
- [ ] 标题、结论条、正文、页底横条没有说同一句话;横条只在需要时用;没有贴着"小结/总结"标签的复述框
- [ ] 完成与计划已用 `status` 分开【已实现】【初步测试】【拟开展】;"全部达标""填补空白""可低成本推广"都有对应证据
- [ ] 数值交代了测试条件、样本量和适用范围;stats 的 `note` 都写了
- [ ] 全篇标题抄成一列后,没有明显重复节拍;bullets 条数不是清一色 3 条
- [ ] 没有修辞性对仗导语(`X之Y：` 三连);导语只做功能标签
- [ ] 每条经验陈述都有证据指针,动词强度与证据强度匹配;目标类数值全篇已声明
- [ ] 演讲者备注写的是"这页的完整论述、怎么讲、怎么答",不是页面文字的复述
- [ ] 技术方法页给出了关键方程/算法,且每条公式有式号或符号表
- [ ] 全文没有卡片、胶囊、圆角框和大面积色块;比较用三线表,过程用流程图,结果用数据图
- [ ] 自动降字号的警告已处理:先删了字,而不是接受缩小的字号
- [ ] slidekit 的填充率警告已清零(<40% 必须处理,40–62% 需给出保留理由)
- [ ] `wordlint.py <deck>.js` 的 hard/always 命中已清零;paper-figures 画的图里的措辞也过了一遍;density 档挑最空的删
- [ ] 图表选型与数据形状匹配;饼图满足三条判据,否则已换水平条
- [ ] 全篇有真实素材(实测图、截图、失败案例,或署名已落页的实景照片);`build()` 的真实素材警告已清零或已显式豁免

### 第 4 步:交付与讲解

交付 `slides/` 目录(slidekit.js + 生成脚本 + 素材 + pptx),说明:改内容只动生成脚本重跑;换主题改一个参数;并用中文讲清本次的版式取舍(为什么这页用三线表、那页用流程图,为什么图缩到这个高度),以及内容审计改了哪些 AI 味表达(用 content-discipline.md 的三列表格)。文献图要列出检索来源,方便用户核对引用。

## 反模式(见到就改)

- 标题写成长结论句或口号句("让仪器像专家一样思考");标题、结论条、正文、页底横条说同一句话
- 内容页没有结论条,或结论条只有口号、没有数字
- 用口号代替具体做法:"赋能""持续进化""最后一公里""构建通用框架"
- 把计划中的工作写成已完成;"全部达标""填补空白""可低成本推广"没有对应证据;数字不交代条件和样本量
- 每页套"基础/核心/延伸""方法/系统/生态"式三分类;用卡片、胶囊、圆角框排版
- 页面只有文字没有图;文献图不注出处;用概念架构图代替实测结果;模拟图、示意图不标注;只讲成功不讲困难
- 29Si、R2、Q3、δ_cal、CO2 这类假上下标
- 一页塞两个论点或两张不相关的图
- 绕过 slidekit 手摆坐标、自造颜色字号;正文字号小于 16 pt;整段加粗;文字放不下直接缩字号而不删字
- 中文正文里混半角标点;标题在奇怪位置断行
- 色带、章节号块、结论横条之外滥用大面积色块;装饰性图标、渐变、行首小色块(AI 味最重的三样)
- 用 `steps` 块硬凑有分支的流程;在 slidekit 里手摆流程图,或把流程图交给生成模型画(文字会错、箭头会歪)——自绘流程图走 paper-figures
- 结尾只有"谢谢"没有可停留的结论页
- 从搜索引擎扒许可不明、不注出处的图;网络图不落署名;用低分辨率/带水印的凑数图
