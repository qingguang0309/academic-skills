# paper-figures 示例:NiFe-LDH OER 四联组图(Angew/wiley 双栏)

skill 端到端测试产物(示例数据,物理量级合理但为演示编造)。
复现:把 `skills/paper-figures/scripts/paperfig.py` 复制到本目录,`python fig1_oer.py`。
覆盖:XRD 堆叠+JCPDS 竖线谱 / XPS 反轴分峰 / LSV η₁₀ 标注 / Tafel 线端标斜率,
以及 `pf.mosaic` 组图、`pf.check_layout` 布局体检(告警清零)、`pf.grayscale` 灰度检查。
